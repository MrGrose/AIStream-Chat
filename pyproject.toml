[project]
name = "aistream-chat"
version = "0.1.0"
description = "AIStream chat"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiofiles>=25.1.0",
    "fastapi[standard]>=0.136.1",
    "gigachat>=0.2.0",
    "pydantic-settings>=2.11.0",
]
